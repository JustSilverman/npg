const mockListen = coroutine(function *(client) {
  //let message
  //message  = yield waitForMessage(client)
  //assertTrue(message, expectedFirstStartup)

  //yield writeMessage(client, sessionConfirmByte)
  //message = yield waitForMessage(client)
  //assertTrue(message, expectedSecondStartup)

  //yield writeMessage(client, setupMessages)
  //yield writeMessage(client, readyForQuery)

  //message = yield waitForMessage(client)
  //while (message !== closeConnection) {
    //assume message is query
    //run query
    //yield writeMessage(client, rowDesc)
    //yield writeMessage(client, rows)
    //yield writeMessage(client, rowEnd)
    //yield writeMessage(client, readyForQuery)
    //yield message = waitForMessage(client)
  // }
})